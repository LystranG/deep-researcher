# 深度研究工作台

本上下文描述一个以空间为边界、支持多轮对话、长期记忆、来源核验和可控扩展的深度研究产品。这里仅定义统一产品语言，不包含实现方案。

## 研究组织

**空间（Workspace）**：
承载一项长期研究工作的顶层边界，包含空间说明、共享知识、空间记忆、会话和已启用扩展。
_Avoid_：项目、知识库、文件夹

**会话（Conversation）**：
空间内一组按时间排序的多轮用户与助手消息；一个空间可以包含多个相互独立但共享空间知识的会话。
_Avoid_：线程、Session、聊天窗口

**消息（Message）**：
会话中由用户、助手或系统产生的一次可引用内容单元。
_Avoid_：Prompt、回答记录

**研究运行（Research Run）**：
由一条用户消息触发、从开始到完成或失败的一次研究过程；同一会话可以产生多次研究运行。
_Avoid_：任务、请求、Agent 会话

**研究计划（Research Plan）**：
归属于单次研究运行、由存在依赖关系且有明确完成条件的有限子任务组成的工作安排；研究计划可以根据失败、证据缺口或证据冲突受控修订。
_Avoid_：固定流程图、工具调用清单、无限任务树

**计划版本（Plan Version）**：
单次研究运行中某一时刻生效的不可变研究计划快照；新版本替代旧版本，但不会改写旧版本及其历史结果。
_Avoid_：Graph 版本、Checkpoint 版本、原地修改

**研究任务（Research Task）**：
研究计划中的有界工作单元，具有明确目标、完成条件、依赖关系、可用工具和预算。
_Avoid_：Agent 类型、Graph 节点、工具调用

**任务结果（Task Outcome）**：
任务控制器根据任务执行事实形成的不可变裁决，表示任务完成、不完整、失败、取消或已被替代。
_Avoid_：模型自报完成、工具观察、研究完整性

**任务结果提案（Task Result Proposal）**：
模型针对研究任务提交的候选结果，必须经过任务控制器的结构、证据和完成条件核验后才能形成任务结果。
_Avoid_：任务结果、停止裁决、模型自报完成

**任务迭代（Task Iteration）**：
研究任务一次有限推进所形成的可审计执行记录，描述本次推进的输入引用、工具活动、用量和继续或等待原因；不等同于模型消息历史。
_Avoid_：Graph 节点、无限循环、思维链

**预算包络（Budget Envelope）**：
研究运行启动时冻结的 token、cost 与绝对 deadline 上限；研究任务还受有限 Model Turn 约束，重规划只能使用剩余额度。
_Avoid_：模型自报预算、Graph recursion limit、可扩容配额

**研究账本（Research Ledger）**：
归属于单次研究运行，记录其研究目标、工作进度、证据积累、覆盖判断和停止原因等可恢复事实；不同研究运行不共用账本，核验后的内容可再成为空间知识或记忆候选。
_Avoid_：空间知识库、长期记忆、Graph 状态

**研究记录（Research Record）**：
由研究运行中已经通过核验的主张及其证据组成、可被同一空间不同会话检索的长期研究知识；研究记录保留来源关系和历史版本，不等同于单次研究账本或长期记忆。
_Avoid_：研究账本、聊天摘要、未经核验的研究结论

**研究结果完整性（Research Completeness）**：
描述研究结果是否覆盖了必要研究目标并具备足够可核验证据；结果可以是完整或明确标注缺口的部分结果。
_Avoid_：模型自信度、回答长度、任务数量

**子代理（Subagent）**：
在研究运行内承担受限角色和子任务的协作者，其权限、预算和生命周期均由研究运行约束。
_Avoid_：无限递归代理、独立用户

## 文件与知识

**会话附件（Conversation Attachment）**：
随消息上传、默认仅在当前会话中使用的图片或文件。
_Avoid_：空间文件、长期知识

**空间文档（Workspace Document）**：
由用户明确加入空间、可被空间内多个会话检索的长期知识来源。
_Avoid_：附件、记忆

**知识来源（Knowledge Source）**：
研究回答所依据的空间文档、会话附件或网页内容。
_Avoid_：记忆、插件

**研究产物（Artifact）**：
研究运行或代码执行生成、可供用户查看或下载的文件、图表或数据结果。
_Avoid_：附件、空间文档

**研究文件空间（Research File Space）**：
代理在研究运行中访问和管理文件的统一视图，区分只读知识来源、当前运行的工作文件与持久研究产物，并保留各自的范围和来源关系。
_Avoid_：模拟文件系统、宿主目录、对象存储

**研究工作文件（Research Work File）**：
归属于单次研究运行、可由子代理和工具修改的中间文件；研究工作文件不会自动成为知识来源或持久研究产物。
_Avoid_：会话附件、空间文档、研究产物

**来源空间（Sources Namespace）**：
研究运行启动时由精确来源版本组成的只读文件视图；运行期间不随会话附件、空间文档或网页来源的后续变化而改变。
_Avoid_：实时附件目录、共享上传目录

**工作空间（Work Namespace）**：
按单次研究运行及研究任务隔离的临时可写文件视图；文件不会自动成为知识来源或持久研究产物。
_Avoid_：共享临时目录、宿主工作目录

**产物空间（Artifacts Namespace）**：
Workspace 级持久研究产物的文件视图；研究工作文件只有经过确定性发布才会在此形成新的产物入口和版本。
_Avoid_：Sandbox 输出目录、来源目录

**文件入口（File Entry）**：
研究文件空间中的稳定逻辑路径身份，记录名称、范围和当前代际，但不承载文件字节内容。
_Avoid_：对象存储 key、宿主绝对路径

**文件版本（File Revision）**：
文件入口在某一时点指向的不可变内容版本，供 Citation、Evidence、Observation 和 provenance 稳定引用。
_Avoid_：可变路径、当前版本号单独引用

**文件 Blob（File Blob）**：
由内容摘要标识的不可变字节内容，可被多个文件版本复用；Blob 身份不构成访问授权。
_Avoid_：文件入口、storage key

**文件能力（File Capability）**：
由确定性控制器授予的、指向特定文件版本或受限路径范围的访问权；能力不能由模型提案自行扩大，也不等同于 Workspace 成员身份。
_Avoid_：对象存储凭证、宿主路径权限

**来源冻结（Source Freeze）**：
研究运行对启动时已有来源版本的固定引用；运行中新增来源只能以新的不可变版本追加，不能改写已冻结版本。
_Avoid_：实时来源查询、可变快照

**文件墓碑（File Tombstone）**：
文件入口被逻辑删除后记录的隐藏状态；墓碑立即隐藏路径和普通检索，但允许用户恢复入口且不删除仍被历史事实引用的文件版本。
_Avoid_：立即物理删除、覆盖历史版本

## 记忆

**会话上下文（Conversation Context）**：
为保持当前会话连贯而使用的近期消息和会话摘要，不跨会话自动生效。
_Avoid_：长期记忆

**历史会话线索（Conversation History Lead）**：
从同一空间其他会话中找回此前讨论的低信任线索，用于定位相关消息或来源；历史会话线索不能单独作为事实证据，也不会自动成为长期记忆。
_Avoid_：共享知识、证据片段、长期记忆

**记忆候选（Memory Candidate）**：
系统从交互中提取、尚未正式影响后续会话的潜在长期信息。
_Avoid_：已保存记忆、聊天摘要

**长期记忆（Long-term Memory）**：
经规则或用户确认后生效、可跨会话召回的用户偏好、空间事实、约束或决策。
_Avoid_：文档原文、完整聊天记录

**记忆范围（Memory Scope）**：
决定长期记忆影响用户、空间还是单个会话的可见边界。
_Avoid_：权限角色

## 引用与核验

**引用（Citation）**：
将助手回答中的具体内容连接到一个可定位知识来源的关系。
_Avoid_：普通链接、参考文献列表

**证据片段（Evidence Span）**：
知识来源中直接支持或反驳一项主张的最小可定位原文范围。
_Avoid_：全文、摘要

**主张（Claim）**：
研究运行准备表达、并且可以由证据支持、反驳或判定证据不足的一项可核验陈述。
_Avoid_：模型观点、未经核验的结论、完整回答

**派生证据（Derived Evidence）**：
由固定文件版本或证据片段经过成功的 Python 沙箱执行产生、可追溯输入与执行记录的证据候选；必须经过确定性核验后才能进入研究账本或支撑研究结论。
_Avoid_：原始来源、无来源计算结果、未经核验的模型结论

**证据缺口（Evidence Gap）**：
某个研究目标仍缺少足够的可核验证据，或关键证据之间存在尚未解决的冲突。
_Avoid_：模型疑问、待办事项、搜索关键词

**覆盖快照（Coverage Snapshot）**：
研究运行在某一时点对各研究目标的证据覆盖、冲突、来源质量和可引用性作出的可审计判断。
_Avoid_：万能置信分数、研究摘要、模型自评

**覆盖矩阵（Evidence Coverage Matrix）**：
Coverage Snapshot 按 Research Plan 的必要 success criteria 逐项记录 covered、missing 或 conflicted，任何必要项缺失或冲突都不能形成完整结果。
_Avoid_：总分置信度、任务数量覆盖

**受控修复（Controlled Repair）**：
模型输出结构不合法时在当前研究任务内进行的一次有限重试；仍不合法则形成任务失败，不启动独立修复循环。
_Avoid_：无限自我修正、普通重试

**证据增益（Evidence Gain）**：
一次任务推进新增了可核验的 Evidence Span、Claim、Artifact Revision 或来源版本；新 URL、搜索摘要和模型措辞变化本身不构成证据增益。
_Avoid_：搜索命中数、Token 消耗、模型新表述

**无进展（No Progress）**：
研究任务连续两次成功推进均未产生证据增益的可审计状态；它是停止或重规划的候选事实，不等同于工具调用失败。
_Avoid_：单次空结果、暂态错误、模型无回复

**核验屏障（Verification Barrier）**：
全局 Verifier 基于一致运行事实形成路由裁决的时点；仅在当前计划阶段的可运行任务全部形成 Task Outcome 后成立。
_Avoid_：每次工具调用后核验、Graph 节点结束、任务运行中断

**停止裁决（Stop Decision）**：
研究运行依据覆盖快照和约束形成的最终停止原因及完整性结论；模型只能提出停止建议，不能单独形成停止裁决。
_Avoid_：Graph 结束、模型完成声明、任务异常

**证据核验（Evidence Check）**：
针对用户选中的回答内容，判断当前可获得证据是支持、反驳、证据不足还是不可核验。
_Avoid_：事实证明、真伪鉴定

## 扩展能力

**工具（Tool）**：
代理在获得权限后可调用的一项外部能力，例如搜索、读取数据或执行代码。
_Avoid_：插件、Skill

**工具调用（Tool Call）**：
模型或系统针对一个工具提出的规范化调用意图，包含工具定义、参数和策略检查结果；它本身不表示工具已经执行成功。
_Avoid_：工具运行、工具观察、模型文本

**工具运行（Tool Run）**：
一次工具调用从领取、执行、重试到终态的可审计生命周期，关联稳定身份、尝试记录和外部请求引用。
_Avoid_：工具调用、Graph 节点、一次网络请求

**工具观察（Tool Observation）**：
一次工具调用返回给代理、可用于后续决策的结构化结果，并保留与该次调用及输出的来源关系。
_Avoid_：工具日志、思维链、证据片段

**运行事件（Run Event）**：
研究运行中已经提交的业务事实变化及其单调顺序，用于审计、恢复和向用户投影；事件重放不会再次执行工具副作用。
_Avoid_：SSE 连接、checkpoint 写入、临时日志

**编排检查点（LangGraph Checkpoint）**：
LangGraph 为恢复控制流保存的内部快照，只保留稳定业务事实的引用和运行游标，不承担研究账本、工具结果或用户可见状态的事实权威。
_Avoid_：研究账本、运行事件、业务数据库

**插件（Plugin）**：
可安装、启用并授予权限的扩展包，为产品提供一个或多个工具或资源。
_Avoid_：Skill、内置功能

**MCP 插件（MCP Plugin）**：
通过 Model Context Protocol 暴露工具、资源或提示的插件类型。
_Avoid_：所有插件、MCP 工具本身

**Skill**：
用于约束代理工作方式、适用场景、允许工具和输出要求的版本化流程说明。
_Avoid_：可执行插件、任意代码

**插件市场（Plugin Marketplace）**：
供用户发现、审阅、安装和管理插件与 Skill 的产品入口。
_Avoid_：工具列表、包仓库

**调用审批（Tool Approval）**：
用户在高风险工具实际执行前，对其目标、权限和预期影响作出的明确授权。
_Avoid_：安装授权、长期权限

## 代码执行

**Python 沙箱（Python Sandbox）**：
用于运行研究代码的隔离执行环境，具有明确的资源、网络、文件和时间边界。
_Avoid_：宿主 Python、普通终端

**Python 执行用途（Python Execution Purpose）**：
Python 沙箱工具调用声明的业务结果类型；`inspect_data` 只产生临时诊断，`transform_artifact` 产生可发布研究产物，`derive_evidence` 基于固定来源产生待核验的派生证据。
_Avoid_：Python 语法模式、权限本身、模型自报结论
