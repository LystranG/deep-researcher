---
status: accepted
---

# 隔离的研究型 Python Sandbox 无需逐次审批

Agent 为满足计算、数据分析或论证需要而运行 Python Sandbox 时无需进入 Tool Approval，因为执行被限制在无网络、非 root、只读根文件系统、显式输入、无宿主凭证并受资源与时间限制的隔离环境中；代码、目的、输入、结果和哈希仍须公开可审计。能够访问外部系统、扩大输入范围或产生外部影响的代码执行不属于此例外，仍须显式授权或审批。
